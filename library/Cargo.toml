[package]
name = "limestone"
description = "Generate short-lived unique program derive address signers"
version = "0.0.0"
edition = "2021"
readme = "README.md"
license-file = "Apache-2.0"

[dependencies]
solana-program = "^1.16"
